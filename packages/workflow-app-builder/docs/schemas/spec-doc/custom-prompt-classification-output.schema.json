{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://composable-workflow.local/schemas/app-builder/spec-doc/custom-prompt-classification-output.schema.json",
  "title": "CustomPromptClassificationOutput",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "intent"
  ],
  "properties": {
    "intent": {
      "type": "string",
      "enum": [
        "clarifying-question",
        "unrelated-question",
        "custom-answer"
      ]
    },
    "customQuestionText": {
      "type": "string",
      "minLength": 1,
      "description": "Question text for either a clarifying-question or unrelated-question intent."
    },
    "customAnswerText": {
      "type": "string",
      "minLength": 1,
      "description": "Custom answer text to buffer with the current numbered response set."
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "intent": {
            "const": "clarifying-question"
          }
        },
        "required": [
          "intent"
        ]
      },
      "then": {
        "required": [
          "customQuestionText"
        ]
      }
    },
    {
      "if": {
        "properties": {
          "intent": {
            "const": "unrelated-question"
          }
        },
        "required": [
          "intent"
        ]
      },
      "then": {
        "required": [
          "customQuestionText"
        ]
      }
    },
    {
      "if": {
        "properties": {
          "intent": {
            "const": "custom-answer"
          }
        },
        "required": [
          "intent"
        ]
      },
      "then": {
        "required": [
          "customAnswerText"
        ]
      }
    }
  ]
}
